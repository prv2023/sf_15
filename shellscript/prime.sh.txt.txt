number=$1
flag=0
for(( i=2;i*i<=number;i++)); do
	if [ $(( number % i )) -eq 0 ]; then
		flag=1
		break
	fi
done

if [ $flag -gt 0 ]; then
	echo "not prime"
else
	echo "prime"
fi

#======= prime.sh =====
